#!/usr/bin/node
/**
* This script prints a multi line string
*/

const lists = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let i = 0; i < lists.length; i++) {
  console.log(lists[i]);
}
